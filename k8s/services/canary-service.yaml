apiVersion: v1
kind: Service
metadata:
  name: fraud-canary
  namespace: fraud-detection
  labels:
    app: fraud-scoring
spec:
  selector:
    app: fraud-scoring
    track: canary
  ports:
    - name: http
      port: 80
      targetPort: 8000
